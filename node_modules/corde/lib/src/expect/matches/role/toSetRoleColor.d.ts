import { ColorResolvable } from "discord.js";
import { IRoleIdentifier, ITestReport } from "../../../types";
import { IExpectTestBaseParams } from "../../../types";
import { ExpectTest } from "../expectTest";
/**
 * @internal
 */
export declare class ToSetRoleColor extends ExpectTest {
  constructor(params: IExpectTestBaseParams);
  action(color: ColorResolvable, roleIdentifier: string | IRoleIdentifier): Promise<ITestReport>;
}
