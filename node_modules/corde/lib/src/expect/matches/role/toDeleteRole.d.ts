import { IRoleIdentifier, ITestReport } from "../../../types";
import { IExpectTestBaseParams } from "../../../types";
import { ExpectTest } from "../expectTest";
/**
 * @internal
 */
export declare class ToDeleteRole extends ExpectTest {
  constructor(params: IExpectTestBaseParams);
  action(roleIdentifier: string | IRoleIdentifier): Promise<ITestReport>;
  private getRoleOrInvalidMessage;
}
