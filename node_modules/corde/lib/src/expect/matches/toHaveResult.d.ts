import { ITestReport } from "../../types";
import { IExpectTestBaseParams } from "../../types";
import { ExpectTest } from "./expectTest";
/**
 * @internal
 */
export declare class IToHaveResult extends ExpectTest {
  constructor(params: IExpectTestBaseParams);
  action(...assertions: (() => Promise<ITestReport>)[]): Promise<ITestReport>;
  private formatFailedTestsMessage;
  private formatReportMessage;
  private filterTestsNames;
}
