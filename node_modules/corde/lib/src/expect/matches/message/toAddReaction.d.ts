import { EmojisType, IMessageIdentifier, ITestReport } from "../../../types";
import { IExpectTestBaseParams } from "../../../types";
import { ExpectTest } from "../expectTest";
/**
 * @internal
 */
export declare class ToAddReaction extends ExpectTest {
  constructor(params: IExpectTestBaseParams);
  action(emojis: EmojisType, messageIdentifier?: IMessageIdentifier | string): Promise<ITestReport>;
}
