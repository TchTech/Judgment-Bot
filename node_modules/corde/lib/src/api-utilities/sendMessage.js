"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true,
});
exports.sendMessage = void 0;

const runtime_1 = require("../common/runtime");

const errors_1 = require("../errors");

function sendMessage(message) {
  if (!message) {
    throw new Error("Can not send a empty message");
  }

  if (!runtime_1.runtime.isBotLoggedIn) {
    throw new errors_1.CordeClientError(
      "Can not send a directly message to channel because the client is not connected yet",
    );
  }

  return runtime_1.runtime.bot.sendMessage(message);
}

exports.sendMessage = sendMessage;
