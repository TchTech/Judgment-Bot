"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true,
});
exports.buildReportMessage = void 0;

const consts_1 = require("../consts");

function buildReportMessage(...messages) {
  let message = "\n";

  for (let i = 0; i < messages.length; i++) {
    if (messages[i]) {
      message += messages[i];
    }
  }

  return addSpace(message);
}

exports.buildReportMessage = buildReportMessage;

function addSpace(value) {
  let newValue = "";
  const split = value.split("\n");

  for (const lineValue of split) {
    newValue += consts_1.EXPECT_RECEIVED_TAB_SPACE + lineValue + "\n";
  }

  return newValue;
}
