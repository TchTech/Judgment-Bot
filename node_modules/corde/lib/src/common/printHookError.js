"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true,
});
exports.printHookErrors = void 0;

const utils_1 = require("../utils");

function printHookErrors(errors, log) {
  for (let i = 0; i < errors.length; i++) {
    if (errors[i].message) {
      print(utils_1.buildReportMessage(errors[i].message), log);
      print(utils_1.buildReportMessage(errors[i].stack), log);
    } else {
      const _formatedObject = utils_1.formatObject(errors[i]);

      print(utils_1.buildReportMessage(_formatedObject));
    }
  }
}

exports.printHookErrors = printHookErrors;

function print(message, log) {
  if (log) {
    log.append(message);
    return;
  }

  console.log(message);
}
